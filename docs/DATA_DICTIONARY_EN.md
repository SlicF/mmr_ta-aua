# DATA_DICTIONARY.md - Ecosystem System Schema Definition

[🇵🇹 Ver Versão em Português](DATA_DICTIONARY.md)

## Core Ecosystem Overview

This operational document explicitly defines mapping sequences generating logical array processing pipelines utilized deeply within the computational processing cores (`extrator.py`, `mmr_taçaua.py`, and `preditor.py`), validating final array outputs executing pure HTML DOM mappings externally.

---

## 1. Hardware Input (Raw Excel Schematics)

Data ingestion structurally originates exclusively from core centralized Excel documents (e.g., `Taça UA 25_26 Oficial.xlsx`).

### Required Matrix Ingestion Columns

To inherently block unresolvable dependencies collapsing matrix operations, calculation mapping arrays definitively demand specific logic nodes tracking physical configurations identically translating the explicit string headers below:

| Explicit Column String | Data Type Required | Structural Restriction | Native Analytical Logic Bound |
| :--- | :--- | :---: | :--- |
| **Jornada** | String / Int | Mandatory | Controls Temporal Mapping Multipliers modulating intense $1.5\times$ scaling execution factors exclusively prioritizing knockout logic variables. |
| **Equipa 1** | String | Mandatory | Origin base canonical node identifier mapped dynamically parsing invalid future match string configurations (e.g. discarding arrays titled "Winner of Qualifier 1"). |
| **Equipa 2** | String | Mandatory | Companion variable translated via `config_cursos.json`. |
| **Golos 1** | Integer | Mandatory | Mathematical output evaluation limits. Unformatted data types inherently trigger node exclusions explicitly protecting prediction parameters. |
| **Golos 2** | Integer | Mandatory | Rival mathematical limit arrays. |
| **Falta de Comparência**| String Variable | Optional | Non-blank arrays systematically tag operations marking them exclusively as Walkover matches (WO). Algorithms forcefully drop parameters protecting highly volatile regression calculations restricting purely onto ELO variations explicitly dropping calibration data dependencies. |
| **Sets 1** | Integer | Volleyball Only | Discrete set limits replacing scalar metrics dynamically mapped bypassing logic dependencies. |

---

## 2. Processed Internal Storage Matrices (.csv)

Data extractors automatically build independent localized pure environments executing standard logic arrays inside `docs/output/csv_modalidades/`. Mapped structures inherently adhere exclusively toward `MODALIDADE_EPOCA.csv` identifiers.

**Normalized Linear Output Headers:**
- **Jornada** -> **Data** -> **Local** -> **Equipa 1** -> **Equipa 2** -> **Golos 1** -> **Golos 2** -> **Sets 1** -> **Sets 2** -> **Falta de Comparência**

Logic actively filters distinct string evaluations effectively merging conflicting nomenclatures mapping entities effectively against configured components.

---

## 3. Frontend Consumable Distribution Models

Decoupled web visual architectures explicitly scrape active distributions calculating isolated configurations mapped against two unique data array targets inherently generated directly via internal automated pipelines.

### A. Evaluated Elo Placement Arrays
Logic mapped executing file queries locally resolving strings located natively throughout: `docs/output/elo_ratings/classificacao_*.csv`

**Generated Output Metrics:**
1. `Posição`: *Integer* - Formatted index placement criteria arrays natively calculating mathematical tie-breaker sequences validating physical results directly calculating explicit numeric outputs.
2. `Equipa`: *String* - Unified Canon logic node string identifiers correctly mapping explicit parameters identifying objects explicitly mapping.
3. `Jogos / Vitórias / Empates / Derrotas`: *Integer* - Pure numeric historical frequency evaluations directly outputting occurrences reliably measuring metrics naturally evaluating arrays natively counting mapped strings natively identifying nodes correctly updating string sequences explicitly indexing arrays mapping entities natively processing outputs reliably representing structures tracking counts dynamically without triggering logical loop conflicts natively mapping independent values successfully storing internal distributions cleanly outputting clean states effectively projecting string models directly interpreting metrics mapping arrays consistently building configurations.
4. `Golos Marcados / Sofridos / Diferença`: *Integer* - Standard performance arrays.
5. `Pontos`: *Float* - Canonical absolute visual ranking values exclusively generated modeling normalized league conditions completely overriding numeric performance parameters isolating explicit scores determining actual rankings explicitly projecting structures.
6. `ELO_Current`: *Float* - Trackable uncoupled variables outputting values independently tracking historical mathematical data parameters isolated mapped evaluations cleanly represented visibly tracking logic mappings correctly representing dynamic updates without failing structures successfully processing variations explicitly isolating parameters.

### B. Predictive Simulation Datasets
Operational matrix configurations generating values explicitly from deeply executed script computations (`preditor.py`) calculating ranges spanning 1M active simulations outputting flat predictive percentages formatting outputs scaling strictly between absolute decimal mappings generating outputs natively representing outcomes completely without additional runtime evaluations cleanly loading arrays mapped against frontend dynamic tooltip interfaces. 
